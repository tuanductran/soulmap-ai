"""Thin command-line entry points for the developer tooling.

Every module here forwards to a canonical implementation elsewhere in
``soulmap.devtools``. Behavior belongs in the implementation module, so these
wrappers stay small enough to read at a glance.
"""
