"""Formatting and linting orchestration.

Wraps Ruff, Pyright, and the Markdown linter behind the single ``soulmap
format`` and ``soulmap lint`` commands so contributors and CI run the same
checks.
"""
