"""Shared input handling for the detector command-line entry points.

Provides the standard-input payload parsing and text normalization helpers that
every detector reuses, so payload validation behaves identically across them.
"""
