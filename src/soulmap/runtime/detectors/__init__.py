"""Framework layer: one detector module per reflective framework.

Each module here pairs with a `skills/frameworks/<name>.md` (or
`skills/spiritual/` for spiritual-discernment) source file and loads its
detection signals through the Library layer in `runtime/knowledge/`. See
docs/engineering/library-vs-framework.md before adding a new detector.
"""
