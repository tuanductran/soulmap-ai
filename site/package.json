{
  "name": "soulmap-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static site for SoulMap AI, generated from the shipped knowledge package.",
  "scripts": {
    "build": "tsx src/build.ts",
    "check": "tsc --noEmit && vitest run && npm run build && tsx src/validate.ts",
    "dev": "tsx src/build.ts && npx --yes serve dist",
    "test": "vitest run"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
