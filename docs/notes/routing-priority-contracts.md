# Routing priority contracts

This branch adds regression coverage for the routing contracts described in
`skills/meta/orchestration.md`. The production selector changes must land in the
same pull request before this branch is merged:

- Stage 1 first and second user messages force Mirror after safety gates.
- HIGH intensity forces De-escalation/Sanctuary with no secondary layer.
- Genuine strong breakthrough insight takes Meaning Integration precedence over
  ordinary topic routes.
- HIGH intensity still outranks insight.

Do not merge the regression tests independently of the production selector fix.
