# What and why

<!-- The problem this solves, and why this approach over the alternatives. -->

## Governance

<!--
Required only when the title carries a P-level tag, for example
"[P1] fix(safety): ...". Delete this section for an untagged pull request.

The placeholders below are deliberately invalid. `p-level-governance` rejects
them, so a tagged pull request cannot merge on the template alone.
-->

- **Priority:** P0 | P1 | P2 | P3
- **Safety boundary:** preserved | changed
- **Evidence:** n/a
- **Rollback:** how to undo this

<!--
Keep the section below only when "Safety boundary" is `changed`. Delete it
otherwise. All four lines are required when it is present.

- ADR: the decision record, or why one is not needed
- Positive regression: the test that fails without this change
- Near-miss regression: the case that must keep passing, so the fix is not
  over-broad
- Safety matrix: the row this affects, or that it is unchanged
-->

## Safety change evidence

- **ADR:**
- **Positive regression:**
- **Near-miss regression:**
- **Safety matrix:**
