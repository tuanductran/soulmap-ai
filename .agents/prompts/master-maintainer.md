# Master Maintainer Prompt

Use this prompt for broad repository maintenance passes in SoulMap AI.

- Start from `project-operating-prompt.md`.
- Use `AGENTS.md` as the baseline SoulMap doctrine.
- Treat `skills/` and `templates/` as shipped source of truth.
- Treat `.agents/` as the canonical local workflow layer.
- Treat `.claude/` and `.codex/` as compatibility layers only.
- Prefer the smallest high-value fix.
- Protect doctrine, safety, packaging consistency, and founder-brand fit.
- Do not replace SoulMap voice with generic product language.
- Run repo checks after meaningful edits.
